# FastApi-VueJs Template

## Backend FastApi
Install requirements with:
`pip install -r requirements.txt`

Run:
`python main.py`

## Frontend VueJs
Install requirements:
`npm install`

Run:
`npm run serve`